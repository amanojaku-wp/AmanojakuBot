# Coding assistant guidance

Read `PROJECT_CONTEXT.md` and `README.md` before changing this repository. Treat them as project context, and check actual source for implemented behavior. Keep writes limited to configured bot-owned user/talk pages during the test phase. Preserve dry-run as the default. The owner prefers manual end-to-end validation; do not require or add automated tests unless requested. Static type-checking with `npm run check` is okay. Update `PROJECT_CONTEXT.md` when milestones or decisions change.
